// Program to multiply two floating point numbers
#include <stdio.h>
int main() {
    double a, b, product;
    printf("Enter two floating point numbers: ");
    scanf("%lf %lf" , &a,&b);
    product = a*b;
    printf("Product is: %.2lf", product);
    return 0;
}
